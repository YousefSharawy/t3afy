import 'package:flutter/material.dart';

class OverviewDivider extends StatelessWidget {
  const OverviewDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
